
<div class="card">
          <div class="main-img-wrapper">
            <img src="{{image_url}}" alt="collection_image">
          </div>
          <div class="info_block">
            <h3 class="name">{{name}}</h3>
          <div class="author">
            <div class="img-wrapper">
              <img src="{{profile_image_url}}" alt="" width="30" height="30">

            </div>
            <p>{{username}}</p>
          </div>
</div>
</div>
